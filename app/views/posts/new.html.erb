<h2 class="text-2xl font-bold mb-4">Criar novo post</h2>

<%= form_with(model: @post, local: true) do |form| %>
  <div class="mb-4">
    <%= form.label :content, "Conteúdo", class: "block text-gray-700 font-semibold mb-2" %>
    <%= form.text_area :content, rows: 5, class: "w-full border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
  </div>

  <div>
    <%= form.submit "Postar", class: "bg-indigo-600 text-white px-6 py-2 rounded hover:bg-indigo-700" %>
  </div>
<% end %>
